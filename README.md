# MP 0 — Reyna Shah

A personal profile card for CS 409, built with HTML and CSS.

## Run locally

```sh
npm install
npm start
```

## Build

```sh
npm run build
```

The GitHub Actions workflow deploys the site to GitHub Pages when changes are pushed to `main`.
